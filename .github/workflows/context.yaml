name: Context
on: push
jobs: 
  print:
    runs-on: ubuntu-latest
    steps:
      - run: echo "${{ github.actor }}"
      - run: echo "${{ github.repository }}"
      - run: echo "${{ github.event }}"